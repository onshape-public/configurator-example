import { ConfiguredPartDirective } from './configured-part.directive';

describe('ConfiguredPartDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfiguredPartDirective();
    expect(directive).toBeTruthy();
  });
});
